part a 



Question 4.1 

A: Linked list is prefferd since we can add/removed dynamicly information.
by using a linked list we can order it according to the prefrences of the deliver destination.

B: Stack: every time we meet an opening bracket we push it, and when we meet a closing one we pop and compare if its from the same type.
at the end the stack need to be empty, this way the order is also checked.

C: Stack, since we can remove easily the last actions (LIFO). because we are demanded to keep only the last 30 actions, when the stack is full and a new action is pushed we throw the oldest one from the bottom.

D: Set: since we dont have duplicate and the order of the players is not important it will be better to use a set.


Question 4.2
the error is *config_numbers[] the user have access to the pointer of the internal array whice mean it can be overwriten by Amit Kmapal (007), he just keeps the pointer and change the values in real time.
in order for this not to happend they should only pass config_numbers[] without the pointer and copy the values into it, this way the internal array stays hidden (encapsulation)



